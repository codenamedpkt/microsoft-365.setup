---
name: 📄 List Current Microsoft Product IDs
on: workflow_dispatch
jobs:
  list-microsoft-product-ids:
    name: 📄 List Product IDs from Microsoft
    runs-on: windows-latest
    steps:
      - name: 📥 Get Product IDs from Microsoft
        id: get-product-id
        shell: pwsh
        run: |
          Install-Module -Name PowerShellGet -Force -AllowClobber -Scope AllUsers -Confirm
          Get-PartnerProduct -ProductID '$productid' | Format-Table -AutoSize
